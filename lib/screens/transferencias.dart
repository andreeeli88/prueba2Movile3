import 'package:flutter/material.dart';

class Transferencias extends StatelessWidget {
  const Transferencias({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Ventanita de transferencias"),
    );
  }
}